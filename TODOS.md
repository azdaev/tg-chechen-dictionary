# TODOS

Отложенное из ревью плана формата выдачи (2026-07-31, `/autoplan`).
План: `~/.gstack/projects/azdaev-tg-chechen-dictionary/main-plan-format-own-db-20260731.md`

## Отложено осознанно

- **Своя база как второй источник (W7).** Решение D1: не строим, пока не сделаны
  три шага — `/stats`, `/missing`, сообщение @movsar42 со списком пробелов и
  вопросом про права на запись. В памяти проекта зафиксировано, что GraphQL
  dosham отдаёт `addEntry`, `updateEntry`, `promote`, `demote` за авторизацией.
  Порог отказа задать до начала работ. Полный список дефектов, обязательных к
  учёту, — в разделе W7 плана.

- **Замена `🇨🇪` в `WordOfDayFooter`** (`internal/net/service.go:52`). Это не
  флаг: C+E — незанятый код ISO 3166-1, рендерится двумя буквенными плитками.
  Замена не выбрана.

## Найдено ревью, вне области текущего плана

- **Один промах разворачивается в 25 запросов к dosham** (`translations.go:182,283`):
  исходный поиск плюс 4 ё-варианта, затем 4 префикса, каждый со своим каскадом.
  Восемь одновременных обработчиков дают 200 запросов по 15 секунд таймаута.
  Лечится singleflight по нормализованному слову плюс семафор на dosham.

- **Отсоединённая запись сохраняет отброшенные результаты**
  (`translations.go:192,428`): каждый ё-вариант планирует запись всего своего
  ответа до того, как фолбэк выберет победителя. Плюс гонка кэша между
  одинаковыми промахами.

- **Остановка бросает крупные отсоединённые пачки** (`main.go:109` ждёт 5 секунд,
  а вложенные AI-задачи живут до 30).

- **`/stats` считает «проверенными» любые строки с выбранным форматированием**
  (`dictionary.go:422-433`) — метрика поощряет число пар, а не точность.

- **Спеллчек отправляется вообще без `ParseMode`** (`spellcheck.go:88`) — та
  единственная карточка, где жирный нёс бы реальную информацию (какие слова
  изменились), не может его использовать.

## Найдено ревью стека формата выдачи (2026-08-02)

- **~5% примеров склеиваются на тильде** (`strings.go`, `getWordBase`). Замер на
  живых данных через `cmd/audit_format -examples`: «силокки», «рисунокке»,
  «обрывокки», «смягчающейее». Два класса — беглые гласные (-ок/-ек) и
  прилагательные на -ей. Не регрессия, потолок помечен `ponytail:`, но ветка
  вывела примеры на гораздо больше карточек, так что дефект стал заметнее.

- **Из API-пути пропал обрез коротких запросов** (`translations.go`,
  `fetchTranslationsFromAPI`). Было: ≤3 букв и ≥10 результатов → срез до 10.
  Замер: «ца» теперь отдаёт 252 пары. Первый результат стал правильнее за счёт
  ранжирования, но кэш-блоб и счётчик на кнопке «Ещё» выросли в 25 раз.

- **`/check` и кнопка проверки орфографии не считают `FreeSpellcheckLimit`**
  (`spellcheck.go`, `runSpellcheck`). Лимит есть только на инлайн-пути. Для
  `/check` так было и раньше, но кнопка после промаха делает бесплатный
  AI-вызов доступным с каждой неудачной попытки перевода.
